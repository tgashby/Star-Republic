#ifndef ANGLE_H
#define ANGLE_H
/* 
 * A representation of the angle between two things, or how fast something
 * is rotating.  
 */

class Angle{
   public:
      //This needs a constructor at some point
      void roll(float degrees);
      void pitch(float degrees);
      void yaw(float degrees);
      
   private:

};

#endif
